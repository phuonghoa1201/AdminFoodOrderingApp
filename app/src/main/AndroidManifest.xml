<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.adminfoodorderingapp">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AdminFoodOrderingApp"
        tools:targetApi="31">
        <activity
            android:name=".model.SignUpActivity"
            android:exported="false" />
        <!-- SplashScreenActivity là hoạt động khởi đầu (LAUNCHER) -->
        <activity
            android:name=".model.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- LoginActivity và MainActivity -->
        <activity
            android:name=".model.LoginActivity"
            android:exported="true" />
        <activity
            android:name=".model.MainActivity"
            android:exported="false" /> <!-- Định nghĩa meta-data -->
        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>